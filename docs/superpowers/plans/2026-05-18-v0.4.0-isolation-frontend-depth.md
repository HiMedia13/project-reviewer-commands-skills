# v0.4.0 Implementation Plan — per-repo isolation, manifest frontend exclusion, deep tech evidence

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Isolate every review run under a per-target slug folder, exclude frontend by manifest signals so the default scope is backend+AI only, and deepen tech-stack evidence to per-technology configuration correctness.

**Architecture:** This is a Claude Code **plugin** (markdown commands/agents + a methodology skill + JSON manifests). There is **no automated test harness** — verification is JSON validity (`python -c "import json; json.load(...)"`), `Grep`/`Read` assertions that the prose changed as specified, and the manual checklist in the spec. Every task is a focused edit on one or two files, a verification step, and a commit. The single source of truth is `skills/review-methodology/SKILL.md`; command/agent files reference it.

**Tech Stack:** Markdown, JSON, the Claude Code plugin format. No build, no runtime deps.

**Spec:** `docs/superpowers/specs/2026-05-18-v0.4.0-isolation-frontend-depth-design.md`

**Commit trailer (every commit in this plan):**
```
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## File Structure

- `skills/review-methodology/SKILL.md` — single source of truth. Gets: a new **Per-target isolation** section, the **Frontend-exclusion rule** section replaced with the manifest-based rule, a short **Depth requirement** note.
- `skills/review-methodology/rubric.md` — short depth-requirement note (no schema fields).
- `commands/review-project.md` — P0 rewritten for slug + per-slug paths + manifest frontend; P4 path lines re-pointed under the slug.
- `commands/review-scope.md` — slug preview + manifest frontend detection report.
- `commands/review-report.md` — optional `[target]` arg, auto-latest selection, legacy fallback, updated argument-hint.
- `agents/review-techstack.md` — per-technology deep-configuration mandate.
- `agents/review-evaluator.md` — verify/downgrade shallow deep claims.
- `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` — version `0.4.0` + description.
- `README.md` — v0.4.0 note, Usage/Outputs/How-it-works, command arg-hints.

Task order: SKILL.md first (source of truth), then the files that must agree with it, manifests, README last. Each task is independently committable.

---

### Task 1: SKILL.md — replace frontend rule, add isolation + depth sections

**Files:**
- Modify: `skills/review-methodology/SKILL.md`

- [ ] **Step 1: Replace the Frontend-exclusion rule section**

Find this exact block (currently lines ~59–68):

```
## Frontend-exclusion rule (faithful to original)

Applied in P0, BEFORE scope/cache, so the incremental diff matches what is
actually evaluated.

- Exclude UI files: extensions `.tsx .jsx .vue .svelte .css .scss .less .html`,
  and `.js/.ts` under directories named `frontend`, `client`, `web`, `ui`,
  `static`.
- UNLESS `--with-frontend` is passed.
- Backend-language files (`.py .go .rs .java .rb .php .cs .kt .scala ...`) are
  **never** excluded regardless of directory.
```

Replace it entirely with:

```
## Frontend-exclusion rule (manifest-based; default = backend + AI only)

Applied in P0, BEFORE scope/cache, so the incremental diff matches what is
actually evaluated. The default review scope is backend + AI only; frontend is
opt-in via `--with-frontend`.

- **Primary detection — manifest signal.** A directory is a *frontend root* if
  it holds a `package.json` whose `dependencies`/`devDependencies` include a
  frontend framework (`react`, `react-dom`, `vue`, `@vue/*`, `next`, `nuxt`,
  `svelte`, `@sveltejs/*`, `@angular/core`, `solid-js`, `preact`) OR it holds a
  frontend bundler/framework config (`vite.config.*`, `next.config.*`,
  `nuxt.config.*`, `svelte.config.*`, `angular.json`, or a `webpack.config.*`
  with a browser/frontend entry). The excluded frontend source = files at or
  under that frontend root with extensions
  `.js .jsx .ts .tsx .vue .svelte .mjs .cjs` plus the always-UI extensions
  `.css .scss .less .html`.
- **Secondary fallback.** For a frontend that ships no manifest, still exclude
  UI files by the legacy heuristic: extensions
  `.tsx .jsx .vue .svelte .css .scss .less .html`, and `.js/.ts` under
  directories named `frontend`, `client`, `web`, `ui`, `static`.
- UNLESS `--with-frontend` is passed (then frontend source is in scope).
- Backend / AI language files
  (`.py .go .rs .java .rb .php .cs .kt .scala .ipynb ...`) are **never**
  excluded regardless of location. The AI part is never excluded.
```

- [ ] **Step 2: Add the Per-target isolation section**

Immediately AFTER the `## Scope rule (faithful to original scope.py)` section
and BEFORE the `## Frontend-exclusion rule` section, insert:

```
## Per-target isolation

All state for a run lives under a per-target slug so multiple repos reviewed
from the same `<workdir>` never collide. Computed in P0, zero LLM:

- Normalize the target string: trim quotes/whitespace, strip a trailing `/`,
  strip a trailing `.git`, strip `protocol://` and any `user[:pass]@`
  credentials, lowercase the host.
- Git URL (`https://host/Owner/Repo.git`, `git@host:Owner/Repo.git`,
  `ssh://…`) → `host_owner_repo` (e.g. `github.com_Owner_Repo`).
- Local path → `local_<basename>` (final path segment).
- Suffix = first 6 lowercase hex of `sha256(normalized full target string)`.
- `slug` = `<sanitized>_<hash6>`; sanitize replaces any char outside
  `[A-Za-z0-9._-]` with `-` and collapses `-` runs. The full-target hash means
  two distinct sources never share a folder even if the readable part matches.

Layout — everything under `<workdir>/<slug>/`:
`last-review.json`, `scope.json`, `output/report-<timestamp>.html`, and (for a
git-URL target only) `.repocache/`. A local-path target is used in place (no
`.repocache/`). Incremental scope reads `<workdir>/<slug>/last-review.json`, so
the prior-SHA diff is always same-repo; if the stored `repo` field still does
not match the resolved target, fall back to `full`.
```

- [ ] **Step 3: Add the Depth requirement note**

In the `## Scoring` section, immediately AFTER the line ending
`...the two are separate axes.` add a new paragraph:

```

**Depth requirement.** Tech-stack judgment is per-technology configuration
correctness, not mere presence. For each major detected technology evaluate its
core configuration (e.g. LLM/agent orchestration → tool/chain/agent definitions
and output parsing; schedulers → job/trigger/timezone/jobstore; ML models →
data provenance, eval-metric meaning, train/serve split — only when the repo
documents it, else state "근거 없음" and do not fabricate). "Declared only, no
deep evidence" must not score `good`.
```

- [ ] **Step 4: Verify**

Run: `Grep` for `Per-target isolation`, `manifest-based`, and `Depth requirement` in `skills/review-methodology/SKILL.md` — expect one match each. Confirm the old `Exclude UI files: extensions` legacy bullet now appears only inside the "Secondary fallback" bullet.

- [ ] **Step 5: Commit**

```bash
git add skills/review-methodology/SKILL.md
git commit -m "v0.4.0 SKILL: manifest frontend rule, per-target isolation, depth requirement

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: rubric.md — depth-requirement note

**Files:**
- Modify: `skills/review-methodology/rubric.md`

- [ ] **Step 1: Add the note**

Find this exact line (currently ~line 68–69):

```
A file or tech with a verified high-severity finding MUST score below 50. Do
not default to high scores — justify the score from the findings.
```

Immediately AFTER it, add:

```

**Depth requirement (tech-stack).** Judge each major technology by its core
configuration correctness, not presence. Examples: LangChain/agent frameworks →
are tools/chains/agents defined with proper schemas and output parsing;
APScheduler/schedulers → job/trigger definitions, timezone, misfire/coalesce,
jobstore; ML/AI models → training-data provenance, evaluation metrics and their
meaning, train/serve separation — only when documented in the repo; if absent,
say "근거 없음", neither fabricate nor over-penalize beyond "undocumented".
Declared-only, no deep evidence → not `good`. No JSON fields are added by this.
```

- [ ] **Step 2: Verify**

Run: `Grep` for `Depth requirement \(tech-stack\)` in `skills/review-methodology/rubric.md` — expect one match. Confirm the JSON schema blocks above are unchanged (no new fields).

- [ ] **Step 3: Commit**

```bash
git add skills/review-methodology/rubric.md
git commit -m "v0.4.0 rubric: add tech-stack depth-requirement note (no schema change)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: review-project.md — P0 slug + per-slug paths + manifest frontend

**Files:**
- Modify: `commands/review-project.md`

- [ ] **Step 1: Replace the entire `## P0 — Resolve & scope (no LLM)` section**

Find the section that begins `## P0 — Resolve & scope (no LLM)` and ends just
before `## P1 — Scanner alone`. Replace the whole P0 section with:

```
## P0 — Resolve & scope (no LLM)
1. Parse: target (first token; local path or git URL), `--max-files N`
   (>=0; reject negative), `--with-frontend`, `--force`, `--workdir DIR`
   (default `.reviewer`).
2. Compute `slug` per the review-methodology **Per-target isolation** rule
   (normalize target → `host_owner_repo` or `local_<basename>`, append 6-hex
   `sha256` of the normalized full target). All paths below are under
   `<workdir>/<slug>/`.
3. Resolve target: local path used in place; git URL → clone or
   fetch+hard-reset into `<workdir>/<slug>/.repocache/`. If resolution/clone
   fails, STOP with a clear message — before any LLM cost.
4. Inventory source files (Glob). Record the resolved commit SHA (`git rev-parse
   HEAD` if a git repo, else `"-"`).
5. Apply the **manifest-based frontend-exclusion rule** from the skill
   (primary = package.json frontend deps / bundler config marks a frontend
   root; secondary = legacy ext/dir fallback; respect `--with-frontend`;
   backend/AI languages incl. `.ipynb` never excluded) — BEFORE scope.
6. Decide scope per the skill: `full` if `--force`, no
   `<workdir>/<slug>/last-review.json`, git diff fails, OR the stored `repo`
   field does not match the resolved target; else `incremental`
   (changed-since-prior-SHA + reverse-import expansion); the rest are `cached`.
7. Apply `--max-files N` (0 ⇒ evaluate nothing). If the in-scope set is large,
   print the count and ask the user to confirm before proceeding.
8. Write the resolved scope to `<workdir>/<slug>/scope.json` (in-scope list,
   mode, frontend-excluded count, detected frontend roots + signal) for
   observability.
9. If the in-scope set is empty, skip P1–P3 and render an empty report in P4.
```

- [ ] **Step 2: Re-point P4 step 3 output path**

In `## P4 — Synthesize & render` step 3, find:

```
   Write `<workdir>/output/report-<timestamp>.html`.
```

Replace with:

```
   Write `<workdir>/<slug>/output/report-<timestamp>.html`.
```

- [ ] **Step 3: Re-point P4 step 5 persistence path**

In `## P4 — Synthesize & render` step 5, find:

```
5. Persist `<workdir>/last-review.json`: `{repo, commit, mode,
```

Replace with:

```
5. Persist `<workdir>/<slug>/last-review.json`: `{repo, commit, mode,
```

- [ ] **Step 4: Verify**

Run: `Grep -n` for `<workdir>/<slug>/` in `commands/review-project.md` — expect matches in P0 steps 3,6,8 and P4 steps 3,5. Run `Grep` for `<workdir>/last-review.json` and `<workdir>/output/` (no `<slug>`) — expect **zero** matches. Run `Grep` for `manifest-based frontend-exclusion` — expect one match.

- [ ] **Step 5: Commit**

```bash
git add commands/review-project.md
git commit -m "v0.4.0 review-project: per-slug paths in P0/P4, manifest frontend rule

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: review-scope.md — slug preview + manifest frontend report

**Files:**
- Modify: `commands/review-scope.md`

- [ ] **Step 1: Replace step 2**

Find (currently step 2):

```
2. Resolve the target: if a local path, use it in place; if a git URL, note
   that a real run would clone into `<workdir>/.repocache/` (do NOT clone here
   — for a URL with no local checkout, report that scope preview needs a local
   path).  `<workdir>` defaults to `.reviewer`.
```

Replace with:

```
2. Compute `slug` per the review-methodology Per-target isolation rule and
   resolve the target: if a local path, use it in place; if a git URL, note
   that a real run would clone into `<workdir>/<slug>/.repocache/` (do NOT
   clone here — for a URL with no local checkout, report that scope preview
   needs a local path). `<workdir>` defaults to `.reviewer`.
```

- [ ] **Step 2: Replace step 4 (frontend rule)**

Find (currently step 4):

```
4. Apply the **frontend-exclusion rule** from the skill (respect
   `--with-frontend`). Backend-language files are never excluded.
```

Replace with:

```
4. Apply the **manifest-based frontend-exclusion rule** from the skill:
   detect frontend roots from `package.json` frontend deps / bundler config
   (primary) with the legacy ext/dir heuristic as fallback; respect
   `--with-frontend`. Backend / AI language files (incl. `.ipynb`) are never
   excluded.
```

- [ ] **Step 3: Replace step 5 (scope decision) to read per-slug**

Find (currently step 5):

```
5. Decide scope: `full` if no `<workdir>/last-review.json`, else `incremental`
   per the skill's scope rule (changed-since-prior-SHA + reverse-import
   expansion). If git diff cannot be computed, fall back to `full`.
```

Replace with:

```
5. Decide scope: `full` if no `<workdir>/<slug>/last-review.json` or its
   stored `repo` differs from the target, else `incremental` per the skill's
   scope rule (changed-since-prior-SHA + reverse-import expansion). If git diff
   cannot be computed, fall back to `full`.
```

- [ ] **Step 4: Extend the printed report (step 7)**

Find (currently step 7's bullet list head):

```
7. Print a concise report ONLY (no files written):
   - target, workdir, mode (full/incremental) and the reason
   - total source files, frontend-excluded count
   - final in-scope count and the in-scope file list
   - cached file count (incremental only)
   - whether `--max-files` further capped the set
```

Replace with:

```
7. Print a concise report ONLY (no files written):
   - target, slug, resolved per-repo workdir `<workdir>/<slug>/`
   - mode (full/incremental) and the reason
   - detected frontend roots and the signal that triggered each (which
     dependency / config file), and the frontend-excluded count
   - total source files
   - final in-scope count and the in-scope file list
   - cached file count (incremental only)
   - whether `--max-files` further capped the set
```

- [ ] **Step 5: Verify**

Run: `Grep` for `<workdir>/<slug>/` and `manifest-based frontend-exclusion` and `slug, resolved per-repo workdir` in `commands/review-scope.md` — expect matches. Run `Grep` for `<workdir>/.repocache/` and `<workdir>/last-review.json` (no `<slug>`) — expect zero matches. Confirm the final line `Never dispatch a subagent. Never write files.` is intact.

- [ ] **Step 6: Commit**

```bash
git add commands/review-scope.md
git commit -m "v0.4.0 review-scope: slug preview, manifest frontend detection report

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: review-report.md — optional [target] arg, auto-latest, legacy fallback

**Files:**
- Modify: `commands/review-report.md`

- [ ] **Step 1: Update the frontmatter argument-hint**

Find the frontmatter block:

```
---
description: Re-render the most recent review result to a fresh self-contained HTML report. Zero LLM calls.
argument-hint: [--workdir DIR]
---
```

Replace with:

```
---
description: Re-render a stored review result to a fresh self-contained HTML report. Optional target selects the repo; otherwise the most recent. Zero LLM calls.
argument-hint: [target] [--workdir DIR]
---
```

- [ ] **Step 2: Replace the Arguments line + step 1**

Find:

```
Arguments: `$ARGUMENTS` (optional `--workdir DIR`, default `.reviewer`).

Steps:
1. Read `<workdir>/last-review.json`. If it does not exist, STOP and tell the
   user clearly: "No prior review found in `<workdir>`. Run `/review-project`
   first." Do nothing else.
```

Replace with:

```
Arguments: `$ARGUMENTS` (optional first positional `target` = the repo URL or
local path that was reviewed; optional `--workdir DIR`, default `.reviewer`).

Steps:
1. Select the stored review under `<workdir>`:
   - If `target` is given, compute `slug` per the review-methodology Per-target
     isolation rule and read `<workdir>/<slug>/last-review.json`.
   - Else pick the most recently modified `<workdir>/*/last-review.json`; print
     which repo was chosen (its stored `repo` field + the slug folder).
   - Legacy fallback: if no `<workdir>/*/last-review.json` exists but a flat
     `<workdir>/last-review.json` does, use it and note it is a legacy
     pre-v0.4.0 file.
   - If none is found, STOP and tell the user clearly: "No prior review found
     in `<workdir>`. Run `/review-project` first." Do nothing else.
```

- [ ] **Step 3: Re-point the written output path (step 5)**

Find:

```
5. Write `<workdir>/output/report-<timestamp>.html` and print its path plus a
```

Replace with:

```
5. Write the report next to the selected `last-review.json`
   (`<workdir>/<slug>/output/report-<timestamp>.html`, or
   `<workdir>/output/...` when a legacy flat file was used) and print its path
   plus a
```

- [ ] **Step 4: Verify**

Run: `Grep` for `argument-hint: \[target\]`, `Per-target isolation rule and read`, `Legacy fallback`, `<workdir>/<slug>/output/` in `commands/review-report.md` — expect matches. Confirm the final line `Never dispatch a subagent. The only file written is the HTML report.` is intact.

- [ ] **Step 5: Commit**

```bash
git add commands/review-report.md
git commit -m "v0.4.0 review-report: optional [target], auto-latest, legacy fallback

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: review-techstack.md — per-technology deep-configuration mandate

**Files:**
- Modify: `agents/review-techstack.md`

- [ ] **Step 1: Insert the deep-configuration paragraph**

Find this exact paragraph (currently the `stack_score is an integer 0-100…`
paragraph) and locate its final sentence ending
`...serious issues accumulate.` Immediately AFTER that paragraph (a blank line,
then the new paragraph, before `Return exactly one JSON object`), insert:

```
Depth requirement: judge each major technology by its CORE CONFIGURATION
CORRECTNESS, not mere presence. Apply the matching pattern when the tech is
present — LLM/agent orchestration (LangChain, LlamaIndex, …): are tools/chains/
agents defined with proper schemas, prompts and output parsers wired, model
calls error-handled; schedulers (APScheduler, Celery beat, …): job/trigger
definitions, timezone, misfire/coalesce, jobstore persistence; ML/AI models:
training-data provenance, evaluation metrics and their meaning, train/serve
separation, reproducibility — ONLY when the repo documents it; if absent, write
"근거 없음" in evidence and neither fabricate nor over-penalize beyond
"undocumented"; web frameworks/ORMs/messaging: equivalent configuration depth.
Put concrete config evidence (file:line, setting values) in `evidence` and the
deep judgment in `rationale`; `used_well`/`stack_score` must reflect it.
"Declared only, no deep evidence" must NOT be `good`.
```

- [ ] **Step 2: Verify**

Run: `Grep` for `Depth requirement: judge each major technology` and `CORE CONFIGURATION` in `agents/review-techstack.md` — expect one match. Confirm the JSON object block (`"purpose": ... "stack_score": 0`) is unchanged and is still the final element. Confirm the Korean free-text instruction line (line ~13) is unchanged.

- [ ] **Step 3: Commit**

```bash
git add agents/review-techstack.md
git commit -m "v0.4.0 review-techstack: per-technology deep-configuration mandate

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: review-evaluator.md — verify/downgrade shallow deep claims

**Files:**
- Modify: `agents/review-evaluator.md`

- [ ] **Step 1: Extend the "challenge unsupported praise" paragraph**

Find this exact sentence inside the paragraph that starts
`Also challenge unsupported praise:`:

```
Rewrite stack_verdict so it accounts for the verified secondary findings; the headline must not contradict high-severity findings.
```

Replace it with:

```
Also verify DEPTH: for each major technology, a `good`/`fit` rating must rest on concrete core-configuration evidence (e.g. LangChain tool/chain definitions, APScheduler trigger/timezone/jobstore, ML model data/eval-metric documentation), not mere presence; web-search the technology's intended configuration when unknown, then downgrade declared-only or shallow ratings. Rewrite stack_verdict so it accounts for the verified secondary findings and the depth assessment; the headline must not contradict high-severity findings.
```

- [ ] **Step 2: Verify**

Run: `Grep` for `Also verify DEPTH:` and `core-configuration evidence` in `agents/review-evaluator.md` — expect one match. Confirm the JSON object block is unchanged and still final, and the Korean free-text instruction line is unchanged.

- [ ] **Step 3: Commit**

```bash
git add agents/review-evaluator.md
git commit -m "v0.4.0 review-evaluator: verify tech-stack depth, downgrade shallow praise

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Version bump + manifest descriptions

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`

- [ ] **Step 1: Edit `.claude-plugin/plugin.json`**

Change `"version": "0.3.0"` to `"version": "0.4.0"`. Change the `description`
value to exactly:

```
Qualitative multi-agent project review: project-level tech-stack-fit assessment with per-technology depth (primary) plus per-file 3-criteria findings (secondary), rendered as a self-contained HTML report. Default scope is backend + AI; frontend is opt-in. Per-target isolation. Does not build/run/test code.
```

- [ ] **Step 2: Edit `.claude-plugin/marketplace.json`**

Change the plugin entry `"version": "0.3.0"` to `"version": "0.4.0"`. Set its
`description` to the same string as Step 1.

- [ ] **Step 3: Verify JSON validity**

Run:
```
python -c "import json; json.load(open('.claude-plugin/plugin.json')); json.load(open('.claude-plugin/marketplace.json')); print('ok')"
```
Expected: `ok`. Then `Grep` for `0.4.0` across `.claude-plugin/` — expect two matches; `Grep` for `0.3.0` — expect zero.

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/plugin.json .claude-plugin/marketplace.json
git commit -m "v0.4.0: bump version, update manifest descriptions

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: README.md — v0.4.0 note, Usage/Outputs/How-it-works, arg-hints

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace the version note block**

Find the `> **v0.3.0** — …` blockquote (lines ~8–11). Replace the whole
blockquote with:

```
> **v0.4.0** — per-target isolation: every run is stored under
> `<workdir>/<slug>/` (slug derived from the repo URL/path) so reviewing
> multiple repos from one folder no longer collides. Default scope is now
> **backend + AI only**; frontend is excluded via manifest signals and
> included only with `--with-frontend`. Tech-stack rows now assess
> per-technology configuration depth, not mere presence. (v0.3.0: fixed the
> 0–100 score scale and separated the tech-stack score from the code-quality
> score; v0.2.0: Korean output; stored JSON stays English.)
```

- [ ] **Step 2: Update the `/review-report` command line**

In the `## Commands` list, find:

```
- `/review-report [--workdir DIR]`
  Cost-free: re-render the most recent result to a fresh HTML report.
```

Replace with:

```
- `/review-report [target] [--workdir DIR]`
  Cost-free: re-render a stored result (the given `target`'s, else the most
  recent) to a fresh HTML report.
```

- [ ] **Step 3: Update the Usage example block**

Find the fenced Usage block:

```
/review-scope D:\path\to\repo                  # 0-cost: preview what gets evaluated
/review-project D:\path\to\repo --max-files 1  # low-cost: validate the pipeline
/review-project D:\path\to\repo                # full qualitative review
/review-report                                 # 0-cost: re-render last result
```

Replace with:

```
/review-scope D:\path\to\repo                  # 0-cost: preview scope + slug
/review-project D:\path\to\repo --max-files 1  # low-cost: validate the pipeline
/review-project D:\path\to\repo                # full review (backend + AI only)
/review-project D:\path\to\repo --with-frontend  # include the frontend too
/review-report                                 # 0-cost: re-render newest repo
/review-report D:\path\to\repo                 # 0-cost: re-render that repo
```

- [ ] **Step 4: Update the Flags paragraph**

Find:

```
Flags: `--with-frontend` (include UI files; backend files are never excluded
either way) · `--force` (ignore cache, full re-eval) · `--max-files N`
(cap evaluated files; `0` = dry run) · `--workdir DIR` (work dir, default
`.reviewer`).
```

Replace with:

```
Default scope is backend + AI only; frontend is detected via manifest signals
(`package.json` frontend deps / bundler config) and excluded unless
`--with-frontend` is passed. Backend / AI files (incl. `.ipynb`) are never
excluded. Flags: `--with-frontend` (also evaluate the frontend) · `--force`
(ignore cache, full re-eval) · `--max-files N` (cap evaluated files; `0` = dry
run) · `--workdir DIR` (work dir, default `.reviewer`).
```

- [ ] **Step 5: Update the Outputs section**

Find:

```
Outputs (under the target's `<workdir>`, default `.reviewer`):
```

Replace with:

```
Outputs (under `<workdir>/<slug>/`, where `slug` is derived from the
target repo URL/path; `<workdir>` default `.reviewer`):
```

- [ ] **Step 6: Update the How it works model-mapping sentence**

Find:

```
mapping: scanner + 4 agents in parallel (tech-stack-fit + 3 per-file
criteria) on `haiku` (cheap bulk), evaluator on `sonnet` (verifies the
primary deliverable). Results persist as `<workdir>/last-review.json`
(latest run only) and `<workdir>/output/report-<ts>.html`. Default workdir
`.reviewer`.
```

Replace with:

```
mapping: scanner + 4 agents in parallel (tech-stack-fit + 3 per-file
criteria) on `haiku` (cheap bulk), evaluator on `sonnet` (verifies the
primary deliverable, including per-technology configuration depth). Results
persist as `<workdir>/<slug>/last-review.json` (latest run per repo only) and
`<workdir>/<slug>/output/report-<ts>.html`. Default workdir `.reviewer`.
```

- [ ] **Step 7: Verify**

Run: `Grep` for `v0.4.0`, `<workdir>/<slug>/`, `backend + AI only`, `/review-report [target]` in `README.md` — expect matches. Run `Grep` for `v0.3.0` — expect exactly one (inside the new note's history tail). Run `Grep` for `the most recent result` — expect zero.

- [ ] **Step 8: Commit**

```bash
git add README.md
git commit -m "v0.4.0 README: isolation, backend+AI default, depth, arg-hints

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: Final consistency sweep

**Files:**
- Read-only: all of the above.

- [ ] **Step 1: Cross-file grep checks**

Run each and confirm:
- `Grep` `<workdir>/last-review.json` across `commands/` — only `commands/review-report.md` may mention it, and only in the **legacy fallback** sentence. Zero elsewhere.
- `Grep` `frontend-exclusion rule (faithful to original)` repo-wide — expect zero (old heading fully replaced).
- `Grep` `0.3.0` across `.claude-plugin/` — expect zero.
- `Grep` `Depth requirement` across `skills/` and `agents/` — expect it in SKILL.md, rubric.md, review-techstack.md (as "Depth requirement:"), and the evaluator's "Also verify DEPTH:".
- `Grep` `--with-frontend` across `commands/`, `skills/`, `README.md` — every mention is consistent with "default excludes frontend, opt-in to include".

- [ ] **Step 2: JSON validity (final)**

Run:
```
python -c "import json; json.load(open('.claude-plugin/plugin.json')); json.load(open('.claude-plugin/marketplace.json')); print('ok')"
```
Expected: `ok`.

- [ ] **Step 3: Commit only if the sweep required a fix**

If Step 1/2 surfaced an inconsistency, fix it and commit:

```bash
git add -A
git commit -m "v0.4.0: consistency sweep fixes

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

If nothing needed fixing, record that the sweep passed and make no commit.

---

## Self-Review (plan author, completed)

**Spec coverage:** A. isolation → Tasks 1 (SKILL section), 3, 4, 5, 9. B. manifest frontend → Tasks 1, 3, 4, 9. C. deep tech evidence → Tasks 1 (depth note), 2, 6, 7. Docs/version → Tasks 8, 9. Manual-verification items in the spec map to the Task 4/5 scope-report and Task 10 sweep. No spec section is unmapped.

**Placeholder scan:** every edit step quotes the exact source text and the exact replacement; no "TBD/handle appropriately/similar to". Verification steps name the literal grep target and expected count.

**Type/identifier consistency:** the term `slug`, the path shape `<workdir>/<slug>/`, the phrases "manifest-based frontend-exclusion rule", "Per-target isolation", and "Depth requirement" are used identically across Tasks 1–10. The manifest description string in Task 8 Steps 1 and 2 is specified as "the same string" to avoid drift.
